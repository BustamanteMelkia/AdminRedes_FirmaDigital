package controller;

import model.client.Authentication;


public class Controller {
    private Authentication auth;
    
    public Controller(){
        
    }
    
    // TODO:
    
    // This method is called when user clicks the getHash button in the view
    public void getHashedUsername(){
        
        // get username from username field in view 
        //String username = usernameField.getText();
        
        //this.auth = new Authentication(username);
        
        // Calculate the username hash
        //String hashedUsername = this.auth.getHashedUsername();
        
        
        // Update the hashedUsername field in the view
        
        // View.hashedUsernameFiel.setText(hashedUsername);
        
    }
}
